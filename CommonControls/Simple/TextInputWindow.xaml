<Window x:Class="CommonControls.Simple.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CommonControls.Simple"
        mc:Ignorable="d"
        Title="TextInputWindow" 
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="TextBoxItem" Margin="2" Grid.Row="0" Width="300"/>

        <Button Margin="2" Grid.Row="1" Width="100" Content="Ok" HorizontalAlignment="Left" Click="Ok_Click"/>
        <Button Margin="2" Grid.Row="1" Width="100" Content="Cancel" HorizontalAlignment="Right" Click="Cancel_Click"/>
    </Grid>
</Window>
