name: Compile Spark

on:
  workflow_dispatch:

jobs:
  windows:
    name: run on windows
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ilammy/msvc-dev-cmd@v1.4.1
    - name: compile
      run: |
        MSUILD "..\..\FBXWRapperDll\FBXWRapperDll.vcxproj\FBXWRapperDll\FBXWRapperDll.vcxproj"
        
        
          