<Window x:Class="CommonControls.BaseDialogs.OpenPackFileOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Open Pack" 
        Width="400"
        Height="180"
        ResizeMode="NoResize"
        Style="{StaticResource CustomWindowStyle}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="How would you like to open?" Margin="0,0,0,8" FontWeight="SemiBold"/>

        <RadioButton x:Name="OpenPackFileRadio" Grid.Row="1" Content="Open pack file (.pack)" 
                     IsChecked="True" Margin="0,0,0,6"/>
        <RadioButton x:Name="OpenFolderRadio" Grid.Row="2" Content="Open system folder as pack" 
                     Margin="0,0,0,6"/>
        <RadioButton x:Name="ConvertRadio" Grid.Row="3" Content="Convert pack file to system folder" 
                     Margin="0,0,0,10"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Content="OK" Margin="0,0,8,0" Click="Ok_Click"/>
            <Button Width="80" Content="Cancel" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
