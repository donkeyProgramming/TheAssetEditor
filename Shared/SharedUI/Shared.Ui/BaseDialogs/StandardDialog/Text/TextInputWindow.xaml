<Window x:Class="CommonControls.BaseDialogs.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:loc="clr-namespace:Shared.Ui.Common"
        mc:Ignorable="d"
        Title="{loc:Loc Dialog.TextInput.Title}" 
        Width="310"
        Height="80"
        ResizeMode="NoResize"
        Style="{StaticResource CustomWindowStyle}"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="TextBoxItem" Margin="2" Grid.Row="0" Width="300" KeyDown="Key_Down"/>

        <Button Margin="2" Grid.Row="1" Width="100" Content="{loc:Loc Dialog.TextInput.OkButton}" HorizontalAlignment="Left" Click="Ok_Click"/>
        <Button Margin="2" Grid.Row="1" Width="100" Content="{loc:Loc Dialog.TextInput.CancelButton}" HorizontalAlignment="Right" Click="Cancel_Click"/>
    </Grid>
</Window>
