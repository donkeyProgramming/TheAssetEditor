<Window x:Class="Shared.Ui.Common.Exceptions.CustomExceptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Shared.Ui.Common.Exceptions"
        xmlns:loc="clr-namespace:Shared.Ui.Common"
        mc:Ignorable="d"
        Style="{StaticResource CustomWindowStyle}"
        Title="{loc:Loc Dialog.Exception.Title}" Height="450" Width="800">
    <DockPanel>

        <Button DockPanel.Dock="Bottom" Content="{loc:Loc Dialog.Exception.ForceCloseButton}" Margin="5" Click="ForceCloseButtonPressed"/>
        <Button DockPanel.Dock="Bottom" Content="{loc:Loc Dialog.Exception.CloseButton}" Margin="5" Click="CloseButtonPressed"/>
        <Button DockPanel.Dock="Bottom" Content="{loc:Loc Dialog.Exception.CopyButton}" Margin="5" Click="CopyButtonPressed" />

        <TextBox 
            IsReadOnly="True"
            Margin="5"
            DockPanel.Dock="Bottom"   
            TextWrapping="Wrap"
            HorizontalContentAlignment="Center"
            Text="{loc:Loc Dialog.Exception.InstructionText}" />


        <Expander DockPanel.Dock="Bottom" IsExpanded="False">
            <Expander.Header>
                <StackPanel Orientation="Horizontal" >
                    <TextBlock Text="{loc:Loc Dialog.Exception.ExtraInformation}" FontWeight="Bold" Margin="3"/>
                </StackPanel>
            </Expander.Header>
            <TextBox x:Name="ExtraInfoHandle" IsReadOnly="True" DockPanel.Dock="Top" Margin="5"/>
        </Expander>

        <TextBlock DockPanel.Dock="TOp" Text="{loc:Loc Dialog.Exception.ErrorMessageLabel}" FontWeight="Bold" Margin="3"/>
        <TextBox x:Name="ErrorTextHandle" IsReadOnly="True"   TextWrapping="Wrap" DockPanel.Dock="Top" Margin="5,0,5,5"/>

    </DockPanel>
</Window>