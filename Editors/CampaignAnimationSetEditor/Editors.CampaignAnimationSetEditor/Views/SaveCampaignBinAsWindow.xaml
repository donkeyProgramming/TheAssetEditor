<Window x:Class="Editors.CampaignAnimationSetEditor.Views.SaveCampaignBinAsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource CustomWindowStyle}"
        Title="Save As" Height="190" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding FolderHint}" TextWrapping="Wrap" Opacity="0.75" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1" Text="File name:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="NameTextBox" Text="{Binding FileName, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Save" Width="90" Margin="0,0,10,0" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="90" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
