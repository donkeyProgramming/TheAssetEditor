<Window 
    x:Class="Editors.Audio.DialogueEventMerger.DialogueEventMergerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:sys="clr-namespace:System;assembly=netstandard"
    xmlns:DialogueEventMerger="clr-namespace:Editors.Audio.DialogueEventMerger"
    xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
    d:DataContext="{d:DesignInstance Type=DialogueEventMerger:DialogueEventMergerViewModel}"
    Style="{StaticResource CustomWindowStyle}"
    mc:Ignorable="d"
    Width="700"
    SizeToContent="Height"
    Closed="OnClosed"
    Title="{loc:Loc DialogueEventMerger.Title}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition
                Height="Auto"/>
            <RowDefinition 
                Height="Auto"/>
            <RowDefinition 
                Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock  
            Grid.Row="0"  
            Text="{loc:Loc DialogueEventMerger.Description}"  
            HorizontalAlignment="Left"
            Margin="10, 10, 10, 0"
            TextWrapping="Wrap"/>

        <Grid 
            Grid.Row="1" 
            Margin="10">

            <Grid.RowDefinitions>
                <RowDefinition 
                    Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid 
                Grid.Row="0">
                
                <Grid.RowDefinitions>
                    <RowDefinition 
                        Height="Auto"/>
                    <RowDefinition 
                        Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition 
                        Width="130"/>
                    <ColumnDefinition 
                        Width="*"/>
                </Grid.ColumnDefinitions>

                <Label
                    Grid.Row="0" 
                    Grid.Column="0" 
                    Content="{loc:Loc DialogueEventMerger.SoundBankSuffix}"
                    ToolTip="{loc:Loc DialogueEventMerger.Tooltips.SoundBankSuffix}"/>

                <TextBox 
                    Grid.Row="0" 
                    Grid.Column="1"
                    Text="{Binding SoundBankSuffix, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    VerticalContentAlignment="Center"
                    Padding="2, 0, 0, 0"/>

                <Label
                    Grid.Row="1" 
                    Grid.Column="0" 
                    Margin="0, 10, 0, 0"
                    Content="{loc:Loc DialogueEventMerger.ModdedSoundBanks}"
                    ToolTip="{loc:Loc DialogueEventMerger.Tooltips.ModdedSoundBanks}"/>

                <ListBox
                    Grid.Row="1" 
                    Grid.Column="1"
                    Margin="0, 10, 0, 0"
                    MaxHeight="500"
                    ItemsSource="{Binding ModdedSoundBanks}"
                    SelectionMode="Extended"
                    VerticalAlignment="Stretch"
                    ScrollViewer.VerticalScrollBarVisibility="Auto">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox 
                                IsChecked="{Binding IsChecked, Mode=TwoWay}">
                                <TextBlock 
                                    Text="{Binding FilePath}" />
                            </CheckBox>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>

        <StackPanel 
            Grid.Row="2" 
            Orientation="Horizontal" 
            HorizontalAlignment="Right" 
            Margin="10, 0, 10, 10">

            <Button 
                Content="{loc:Loc DialogueEventMerger.Ok}"
                Width="75"
                Margin="0, 0, 5, 0"
                Command="{Binding GenerateMergedDialogueEventSoundBankCommand}" 
                IsEnabled="{Binding IsOkButtonEnabled}"/>

            <Button 
                Content="{loc:Loc DialogueEventMerger.Cancel}" 
                Width="75"
                Margin="5, 0, 0, 0" 
                Command="{Binding CloseWindowActionCommand}"/>
        </StackPanel>
    </Grid>
</Window>