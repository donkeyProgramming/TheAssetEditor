<UserControl x:Class="AnimMetaEditor.Views.Editor.MainEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AnimMetaEditor.Views.Editor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <local:EditorToolBar Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" />
        <local:EditorTagExplorer Grid.Column="0" Grid.ColumnSpan="1" Grid.Row="1"/>
        <local:EditorValueEditor Grid.Column="1" Grid.ColumnSpan="1" Grid.Row="1"/>
    </Grid>
</UserControl>
